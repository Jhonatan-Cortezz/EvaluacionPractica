<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:padding="40dp"
    android:background="#DAF8AF40"
    android:orientation="vertical"
    android:layout_height="match_parent"
    tools:context=".RegistrarCategoria">

    <EditText
        android:id="@+id/codCate"
        android:layout_marginTop="20dp"
        android:layout_gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:inputType="number"
        android:background="@drawable/redondo"
        android:hint="codigo categoria" />

    <EditText
        android:id="@+id/nomCat"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:inputType="textPersonName"
        android:background="@drawable/redondo"
        android:hint="nombre categoria" />

    <EditText
        android:layout_marginTop="20dp"
        android:id="@+id/estadoCat"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:inputType="number"
        android:background="@drawable/redondo"
        android:hint="estado categoria" />

    <Button
        android:layout_marginTop="30dp"
        android:id="@+id/registraCat"
        android:text="Guardar"
        android:drawablePadding="0dp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:padding="20dp"
        android:drawableStart="@drawable/ic_save"
        android:background="@color/colorPrimary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</androidx.appcompat.widget.LinearLayoutCompat>